#include "uart_regs.h"

.data
string:
	.ascii "Hello World!"
	.byte 0x0D
	.byte 0x0A
	.byte 0x00
.text

.macro uart_print

init:
	ldr r0, =uart_TX_RX_FIFO0
	ldr r1, =uart_Channel_sts_reg0
	ldr r2, =string

TRANSMIT_loop:


.endm
