int csd_main()
{
	unsigned int * sw;
	sw = (unsigned *)0x41210000;

	long long epoch = 0;

	if ((*sw) == 0x0)
		epoch = 50000000000;
	else if ((*sw) == 0x1)
		epoch = 1000000;

	for (int i = 0; i < epoch;)
		i++;

	return 0;
}
